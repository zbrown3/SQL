SELECT *
FROM movies
WHERE imdb_rating > 8;

-- Query results can be restricted using the WHERE clause in order to obtain only the information we want.
-- WHERE clause filters the result set to only include rows where the following condition is true.